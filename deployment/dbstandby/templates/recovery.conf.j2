# WARNING: This is a generated file. Ansible will overwrite it.
# {{ansible_managed}}
standby_mode = 'on'
primary_conninfo = 'host={{ db_master_ip }} user={{ pg_replication_user }} password={{ pg_replication_password }}'
# TODO: use this to enable SSL, requires CA-setup, or perhaps just startssl?
#primary_conninfo = 'host={{ db_master_ip }} user={{ pg_replication_user }} password={{ pg_replication_password }} sslmode=verify-ca'

